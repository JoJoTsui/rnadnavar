# SEQC2 indel-policy decision (2026-09-14)

This is a bounded, non-HG008 decision from preserved `som.py` metrics. It
compares the current threshold consensus candidate with the DNA DeepSomatic
reference in all four available cells. No mapping, calling, realignment, or
source-cache mutation was performed.

## Rule tested

A shared candidate qualifies only if, in every cell, it never loses a TP, never
adds an FP, and improves at least one cell. Dataset-specific thresholds and
copying the strongest caller are prohibited.

| Cell | Candidate TP/FP/FN | DeepSomatic TP/FP/FN | Non-regression |
|---|---:|---:|---|
| WES-LL / UKB | 35 / 1 / 60 | 41 / 4 / 54 | No (TP loss) |
| WES-LL / MedExome | 19 / 1 / 15 | 23 / 2 / 11 | No (TP loss) |
| WGS-IL / UKB | 73 / 4 / 22 | 85 / 11 / 10 | No (TP loss) |
| WGS-IL / MedExome | 25 / 1 / 9 | 28 / 4 / 6 | No (TP loss) |

The machine-readable result is generated by
`examples/seqc2/scripts/build_indel_policy_matrix.py`; the ignored comparison
output is under `examples/seqc2/comparison/common_policy_20260914/`. The result
is `no_qualifying_shared_rule`, so the retained policy is the existing
`threshold_consensus` behavior. This is a conservative retention decision, not
a claim that threshold consensus beats DeepSomatic.

The decision remains subject to independent HG008 validation; HG008 is not
used to tune the rule and was not inspected or modified.
